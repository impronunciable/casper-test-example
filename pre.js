
/**
 * Pre
 */

// Constants
casper.BASE_URL = 'http://wolframalpha.com';
casper.CAPTURES_DIR = 'captures/';

// Viewport size
casper.viewportSize = { width: 800, height: 600 };

// Start the suite
casper.echo("About to start the suite.");
casper.test.done();
